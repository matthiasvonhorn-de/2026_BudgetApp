name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]

jobs:
  lint-and-unit:
    name: Lint & Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate
      - name: Lint (non-blocking — pre-existing issues)
        run: npm run lint
        continue-on-error: true
      - run: npm run test:unit

  # E2E tests require a running app + database — will be configured in test strategy Part 2
  # e2e:
  #   name: E2E Tests
  #   needs: [lint-and-unit]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: npm
  #     - run: npm ci
  #     - run: npx prisma generate
  #     - run: npx playwright install --with-deps chromium
  #     - run: npm run build
  #     - run: npm run test:e2e
